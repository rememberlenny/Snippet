<h1>Fitness activities in East Harlem</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Info</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @recreations.each do |recreation| %>
      <tr>
        <td><%= recreation.name %></td>
        <td><%= recreation.address %></td>
        <td><%= recreation.info %></td>
        <td><%= link_to 'Show', recreation %></td>
        <td><%= link_to 'Edit', edit_recreation_path(recreation) %></td>
        <td><%= link_to 'Delete', recreation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add Activity', new_recreation_path %>
